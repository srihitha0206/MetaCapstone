import React from 'react';
import Nav from './Nav.js';

const Header = () => {
  return (
    <header>
      <img src="Logo.svg" alt="logo"></img>
      <Nav />
    </header>
  );
};

export default Header;
